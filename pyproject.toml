[tool.pytest.ini_options]
addopts = "--cov=. --cov-report=term-missing --cov-report=html:htmlcov --cov-report=xml"

[tool.coverage.run]
source = ["."]
omit = [
    "tests/*",
    "htmlcov/*",
]

[tool.coverage.report]
exclude_lines = [
    "if __name__ == .__main__.:",
]
